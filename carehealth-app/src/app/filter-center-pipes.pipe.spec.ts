import { FilterCenterPipesPipe } from './filter-center-pipes.pipe';

describe('FilterCenterPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCenterPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
